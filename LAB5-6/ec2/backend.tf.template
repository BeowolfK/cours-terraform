# Backend S3 pour Student Y (9) - EC2 State
terraform {
  backend "s3" {
    bucket         = "terraform-state-agvq0"
    key            = "global/s3/student_7_9/ec2.tfstate"
    region         = "eu-west-3"
    dynamodb_table = "terraform-up-and-running-locks"
    encrypt        = true

    # TODO: Copier ce fichier vers backend.tf et ajouter vos credentials:
    # access_key = "VOTRE_ACCESS_KEY"
    # secret_key = "VOTRE_SECRET_KEY"
  }
}
