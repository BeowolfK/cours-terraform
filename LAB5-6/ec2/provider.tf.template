# Provider AWS
terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "eu-west-3"

  # TODO: Copier ce fichier vers provider.tf et ajouter vos credentials:
  # access_key = "VOTRE_ACCESS_KEY"
  # secret_key = "VOTRE_SECRET_KEY"
}
